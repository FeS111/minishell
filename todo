replace in cmd too

count pipes and take the dup2 to stdin only the amount of pipes right now its not handled for every instance for example cat | grep m | wc -l < file1 because i only check if there ist table next element
